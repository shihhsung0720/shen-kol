# CLAUDE.md — 給 AI 協作者的規則

## 第一層：紅線（違反就停下、不要動手）

1. **不編造事實**：所有品牌 / KOL / 案例的事實、以 `brand-brain/` 和 `data/` 內檔案為準。
   沒檔案的事就回答「不知道」。AI 印象中的網紅 / 資料、不算來源。
2. **動手前先講方案**：新增 / 修改 / 刪除任何資料、先一句話講要改什麼、等使用者確認再動。
3. **不可逆動作必須問**：刪檔、改 `.github/`、改權限、`git push --force`、close issue、
   都要先問。
4. **只改被要求的部分**：不要順手改旁邊欄位、不要順手「整理」格式、不要順手改 README。
   連動到別處先列出來問。
5. **修 bug 先想規則、再想 code**：能用 issue template / schema / 缺欄位攔截下、
   就不要寫 GitHub Action 或腳本。
6. **金鑰永遠不放對話 / 程式碼 / commit**：API key、PAT、cookie、session token、密碼
   一律走 `.env` 或 GitHub Secrets。看到使用者貼出疑似密鑰、立刻警告 + 拒絕使用 +
   提示撤銷。
7. **保養品宣稱走法規**：療效、敏感肌、抗老、修復等字眼、以 `brand-brain/brand.md [4]
   法規邊界` 為準。不在邊界內的詞、AI 不可生成、不可「優化」使用者的話越界。

## 第二層：資料地圖（哪個檔說了算）

- **品牌靈魂 / 哲學 / 調性 / 法規** → `brand-brain/brand.md`
- **產品線（名稱、成分、宣稱）** → `brand-brain/products.md`
- **過往 Reels + KOL 合作案例** → `brand-brain/cases.md`
- **載入規則 + 更新觸發** → `brand-brain/index.md`（總控）
- **KOL 名單真實狀態** → `data/kols/<slug>.yml`（之後建）
- **邀約進度** → `data/outreach/<date>-<slug>.yml`（之後建）
- **個人化偏好（不通用）** → `CLAUDE.local.md`

「最新貼文」「最近合作」這類動態事實：除非在 `cases.md` 有紀錄、否則回答「未紀錄」。

## 第三層：日常習慣

- **任務切換開新對話**：找 KOL、寫邀約話術、寫 Reels 腳本、改 schema、是不同的事、
  分開對話、context 不要混。
- **方向錯了 rewind**：不要在錯的方向上加東西補救。
- **警告類機制走完四階段**：警告 → 自動修補 → 通知使用者 → 擋住新任務。
  只印警告不修、三個月後就變噪音。
- **新規則先當軟規則**：踩到坑先記到下面「觀察中」、反覆出現再升級成紅線。

## 第四層：拒絕生稿條件

`brand-brain/brand.md` 缺以下任一節、AI **不可生成** Reels 或邀約話術：
- [0] 基本資料
- [1] 品牌哲學
- [3] 內容調性

回應使用者：「品牌大腦缺 X 節、請先填、再生稿」。

## 觀察中（軟規則、還沒升級）

(空)
